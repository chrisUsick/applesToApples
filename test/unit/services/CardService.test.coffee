chai = require 'chai'
P = require 'bluebird'
chai.should()
describe 'CardService', ->
  describe '#getRedCards', (done) ->
    it 'should return a list of cards'
    it 'should add cards to used cards set'
